# MindCampus 心理测评模块开发文档

## 📋 项目概述

本文档介绍了 MindCampus 高校学生心理健康智能干预平台的心理测评模块（Assessment Module）的完整实现，包括前后端代码开发。

**开发日期**: 2025-11-04  
**开发范围**: `/pages/assessment/list` 测评列表页面及相关前后端功能  
**技术栈**: SpringBoot 3.5.4 + uni-app + MyBatis Plus + Vue 2

---

## 🎯 功能特性

### 核心功能
1. ✅ **测评列表展示** - 待填问卷和已完成问卷分类展示
2. ✅ **统计数据** - 实时展示待填、已完成和总计数量
3. ✅ **问卷答题** - 支持选择题和简答题两种题型
4. ✅ **自动评分** - 选择题自动判分，简答题默认给分
5. ✅ **结果展示** - 详细的测评结果和答题详情
6. ✅ **风险评估** - 根据得分自动评估风险等级（低/中/高）

---

## 📂 文件结构

### 后端文件（Java）

```
mc-project/src/main/java/com/mc/evaluation/
├── controller/
│   └── AppAssessmentController.java          # APP端心理测评控制器
├── domain/
│   ├── QuestionnaireAnswer.java              # 答题记录实体（已存在）
│   └── vo/
│       ├── AssessmentStatisticsVO.java       # 统计数据VO（新增）
│       ├── QuestionnaireListVO.java          # 问卷列表VO（新增）
│       ├── QuestionnaireItemVO.java          # 问卷列表项VO（新增）
│       └── EvaluationResultVO.java           # 结果详情VO（已存在）
├── mapper/
│   └── QuestionnaireAnswerMapper.java        # 答题记录Mapper（新增）
├── service/
│   ├── IAppAssessmentService.java            # APP端服务接口（新增）
│   └── impl/
│       └── AppAssessmentServiceImpl.java     # APP端服务实现（新增）
└── resources/mapper/evaluation/
    └── QuestionnaireAnswerMapper.xml         # MyBatis映射文件（新增）
```

### 前端文件（uni-app）

```
mc-ui/MindCampus-App/
├── api/
│   └── assessment.js                         # API接口封装（更新）
└── pages/assessment/
    ├── list.vue                              # 测评列表页面（更新）
    ├── detail.vue                            # 答题页面（新增）
    └── result.vue                            # 结果页面（新增）
```

---

## 🔌 API 接口文档

### 基础路径
```
/app/assessment
```

### 接口列表

#### 1. 获取测评统计数据
```http
GET /app/assessment/statistics
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "pending": 3,
    "completed": 5,
    "total": 8
  }
}
```

---

#### 2. 查询问卷列表
```http
GET /app/assessment/questionnaires
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "pendingList": [
      {
        "questionnaireId": 1,
        "title": "抑郁症筛查量表 (PHQ-9)",
        "description": "评估近两周的情绪状态",
        "questionCount": 10,
        "duration": 10,
        "publisher": "张老师",
        "deadline": "2025-01-25",
        "type": "0"
      }
    ],
    "completedList": [
      {
        "questionnaireId": 2,
        "title": "焦虑自评量表",
        "completedTime": "2025-01-10",
        "questionCount": 8,
        "score": 72,
        "resultId": 101,
        "riskLevel": "低"
      }
    ]
  }
}
```

---

#### 3. 查询问卷详情（包含题目）
```http
GET /app/assessment/questionnaire/{questionnaireId}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "questionnaireId": 1,
    "title": "抑郁症筛查量表",
    "description": "评估说明",
    "totalScore": 100,
    "questions": [
      {
        "questionId": 1,
        "type": "choice",
        "content": "你最近感觉如何？",
        "options": "{\"A\":\"很好\",\"B\":\"一般\",\"C\":\"不好\"}",
        "standardAnswer": "A",
        "score": 10,
        "orderNum": 1
      }
    ]
  }
}
```

---

#### 4. 提交答题
```http
POST /app/assessment/submit
Content-Type: application/json
```

**请求示例**:
```json
{
  "questionnaireId": 1,
  "answers": [
    {
      "questionId": 1,
      "userAnswer": "A"
    },
    {
      "questionId": 2,
      "userAnswer": "这是我的回答内容"
    }
  ]
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": 101  // resultId
}
```

---

#### 5. 查询测评结果详情
```http
GET /app/assessment/result/{resultId}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "result": {
      "resultId": 101,
      "studentId": 1,
      "studentName": "张三",
      "questionnaireId": 1,
      "questionnaireTitle": "抑郁症筛查量表",
      "totalScore": 85,
      "riskLevel": "低",
      "aiAnalysis": "AI分析内容",
      "createTime": "2025-01-10 10:30:00"
    },
    "answers": [
      {
        "answerId": 1,
        "questionId": 1,
        "content": "你最近感觉如何？",
        "type": "choice",
        "standardAnswer": "A",
        "userAnswer": "A",
        "score": 10,
        "obtainScore": 10,
        "isCorrect": 1
      }
    ],
    "totalQuestions": 10,
    "correctCount": 8,
    "accuracy": "80.00%"
  }
}
```

---

#### 6. 查询我的测评历史
```http
GET /app/assessment/my-results
```

---

#### 7. 检查问卷是否已完成
```http
GET /app/assessment/check/{questionnaireId}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": true
}
```

---

## 💾 数据库表结构

### 1. evaluation_result (心理测评结果表)

```sql
CREATE TABLE evaluation_result (
    result_id BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id BIGINT NOT NULL,
    student_name VARCHAR(50),
    questionnaire_id BIGINT NOT NULL,
    questionnaire_title VARCHAR(200),
    total_score BIGINT DEFAULT 0,
    risk_level VARCHAR(20),
    ai_analysis TEXT,
    ai_status VARCHAR(1) DEFAULT '0',
    read_status VARCHAR(1) DEFAULT '0',
    create_by VARCHAR(64),
    create_time DATETIME,
    update_by VARCHAR(64),
    update_time DATETIME,
    remark VARCHAR(200)
) COMMENT='心理测评结果表';
```

### 2. questionnaire_answer (答题记录表)

```sql
CREATE TABLE questionnaire_answer (
    answer_id BIGINT PRIMARY KEY AUTO_INCREMENT,
    result_id BIGINT NOT NULL,
    questionnaire_id BIGINT NOT NULL,
    question_id BIGINT NOT NULL,
    -- 冗余字段
    type VARCHAR(20) NOT NULL,
    content VARCHAR(500) NOT NULL,
    options JSON,
    standard_answer VARCHAR(200),
    score INT DEFAULT 0,
    -- 用户作答
    user_answer VARCHAR(500),
    is_correct TINYINT(1),
    obtain_score INT DEFAULT 0,
    create_by VARCHAR(64),
    create_time DATETIME,
    update_by VARCHAR(64),
    update_time DATETIME,
    remark VARCHAR(200)
) COMMENT='心理测评答题记录表';
```

---

## 🎨 前端页面说明

### 1. 测评列表页面 (`list.vue`)

**功能特性**:
- 顶部统计卡片展示待填、已完成、总计数量
- 待填问卷列表：显示问卷标题、描述、题数、时长、截止时间
- 已完成列表：显示完成时间、得分、查看结果按钮
- 下拉刷新功能
- 点击跳转到答题页面或结果页面

**页面状态**:
```javascript
{
  stats: { pending, completed, total },
  pendingList: [],     // 待填问卷数组
  completedList: []    // 已完成问卷数组
}
```

---

### 2. 答题页面 (`detail.vue`)

**功能特性**:
- 问卷基本信息展示
- 实时答题进度条
- 选择题：单选按钮样式
- 简答题：多行文本输入框，字数统计
- 提交前验证是否全部作答
- 提交确认对话框
- 离开页面提醒

**页面状态**:
```javascript
{
  questionnaire: {},   // 问卷信息
  questions: [],       // 题目列表
  answers: {}          // 答题记录 { questionId: answer }
}
```

**计算属性**:
- `answeredCount`: 已回答题数
- `progressPercent`: 进度百分比
- `isAllAnswered`: 是否全部作答

---

### 3. 结果页面 (`result.vue`)

**功能特性**:
- 结果概览：表情图标、得分、风险等级
- 统计信息：总题数、正确题数、准确率
- AI分析报告（如果有）
- 答题详情：每题的标准答案和用户答案对比
- 选择题显示对错标识
- 底部操作：返回列表、分享结果

**页面状态**:
```javascript
{
  resultData: {
    result: {},        // 测评结果
    answers: [],       // 答题记录
    totalQuestions,    // 总题数
    correctCount,      // 正确题数
    accuracy          // 准确率
  }
}
```

---

## 🔧 核心业务逻辑

### 1. 用户ID与学生ID映射

系统中存在两种ID：
- **userId**: 系统用户表（sys_user）的主键，通过 `SecurityUtils.getUserId()` 获取
- **studentId**: 学生信息表（student_info）的主键，用于业务逻辑

**映射关系**:
```
student_info 表
├── student_id (主键，学生ID)
└── user_id (外键，关联 sys_user.user_id)
```

**处理流程**:
```java
// Controller层获取userId
Long userId = SecurityUtils.getUserId();

// Service层通过userId查询studentId
private Long getStudentIdByUserId(Long userId) {
    LambdaQueryWrapper<Student> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Student::getUserId, userId);
    Student student = studentInfoMapper.selectOne(wrapper);
    
    if (student == null) {
        throw new ServiceException("学生信息不存在，请先完善学生档案");
    }
    
    return student.getStudentId();
}
```

**注意事项**:
- 所有测评相关操作都使用 `studentId`，而不是 `userId`
- 如果学生未完善档案（student_info表中无记录），会抛出异常提示
- 这种设计支持一个系统用户对应一个学生档案的映射关系

---

### 2. 评分逻辑

```java
// 选择题：对比标准答案
if ("choice".equals(question.getType())) {
    boolean isCorrect = standardAnswer.equalsIgnoreCase(userAnswer);
    obtainScore = isCorrect ? question.getScore() : 0;
}

// 简答题：默认给满分
else {
    obtainScore = question.getScore();
}
```

### 3. 风险等级计算

```java
double percentage = (double) score / totalScore;

if (percentage >= 0.8) {
    return "低";
} else if (percentage >= 0.5) {
    return "中";
} else {
    return "高";
}
```

### 4. 数据冗余设计

为了历史追溯和快速统计，`questionnaire_answer` 表中冗余了题目信息：
- `type`: 题目类型
- `content`: 题干内容
- `options`: 选项（JSON格式）
- `standard_answer`: 标准答案
- `score`: 分值

**优点**:
- 即使题目被修改或删除，历史答题记录仍可完整展示
- 无需关联查询，提高查询效率

---

## 🚀 部署说明

### 后端部署

1. **确保数据库表已创建**
   - `evaluation_result`
   - `questionnaire_answer`

2. **Maven 构建**
   ```bash
   cd MindCampus
   mvn clean package -DskipTests
   ```

3. **启动应用**
   ```bash
   java -jar mc-admin/target/mc-admin.jar
   ```

### 前端部署

1. **安装依赖** (如需要)
   ```bash
   cd mc-ui/MindCampus-App
   npm install
   ```

2. **运行项目**
   - HBuilderX: 运行 -> 运行到浏览器/模拟器
   - 命令行: `npm run dev:h5`

---

## 🧪 测试建议

### 功能测试清单

- [ ] 列表加载正常，统计数据准确
- [ ] 待填问卷点击可进入答题页面
- [ ] 选择题可正常选择，简答题可正常输入
- [ ] 未完成全部题目时提交按钮禁用
- [ ] 提交成功后跳转到结果页面
- [ ] 结果页面数据展示正确
- [ ] 已完成问卷可查看结果
- [ ] 不能重复提交同一问卷
- [ ] 下拉刷新功能正常
- [ ] 返回按钮逻辑正确

### 边界测试

- [ ] 问卷列表为空时的显示
- [ ] 题目包含特殊字符
- [ ] 答案包含特殊字符
- [ ] 网络异常时的错误提示
- [ ] 并发提交的处理

---

## 📝 注意事项

1. **权限控制**: 当前接口未添加 `@PreAuthorize` 注解，生产环境需要添加权限控制
2. **事务处理**: `submitAnswer` 方法已添加事务注解，确保数据一致性
3. **防重复提交**: 通过 `checkCompleted` 方法检查是否已提交
4. **数据验证**: 前端做了基本验证，后端也需要添加参数校验
5. **AI分析**: 当前 `aiAnalysis` 字段为空，需要集成 AI 模块
6. **图片上传**: 如需支持图片题，需要扩展题目类型和答案存储

---

## 🔮 未来优化方向

1. **实时保存**: 答题过程中自动保存草稿
2. **计时器**: 添加答题计时功能
3. **题目乱序**: 支持题目随机打乱
4. **多选题**: 扩展支持多选题型
5. **图片题**: 支持图片类型的题目和答案
6. **AI分析**: 接入 AI 模块自动生成分析报告
7. **分享功能**: 实现结果分享到社交平台
8. **历史对比**: 多次测评结果对比分析
9. **消息推送**: 新问卷发布时推送通知
10. **离线答题**: 支持离线答题，联网后同步

---

## 📧 联系方式

如有问题，请联系项目负责人。

**开发者**: Cursor AI Assistant  
**项目**: MindCampus v3.9.0  
**文档版本**: 1.0  
**最后更新**: 2025-11-04

