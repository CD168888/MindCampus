# MindCampus 首页优化文档

## 📋 优化概述

基于大厂级 UI 设计标准，对 MindCampus 首页进行了全面重构，实现了现代化、专业化的心理健康服务平台界面。

**核心设计理念**：采用**温暖治愈系**配色方案，以翡翠绿为主色调，摒弃传统蓝色的冷冰冰感觉，给用户带来温暖、自然、充满希望的体验。详见 [配色方案说明](./COLOR_SCHEME.md)。

## 🎨 设计规范

### 颜色系统（温暖治愈系）

#### 主色调（1-2种）
- **主品牌色**: `#10b981` - 翡翠绿，传达生命、希望与疗愈感
- **辅助色**: `#fb923c` - 温暖橙，代表活力、温暖与积极

#### 中性色（不超过3种）
- **深灰**: `#1f2937` - 主标题文字
- **中灰**: `#6b7280` - 描述文字
- **浅灰**: `#f9fafb` - 背景色

#### 功能色
- 成功: `#10b981` (翡翠绿)
- 警告: `#fbbf24` (金黄色)
- 错误: `#f87171` (柔和红)

### 圆角系统
- 小圆角: `8rpx`
- 基础圆角: `12rpx`
- 中等圆角: `16rpx` (卡片常用)
- 大圆角: `20rpx`
- 完全圆角: `9999rpx` (按钮、头像)

### 阴影系统
```scss
// 轻微阴影（卡片）
box-shadow: 0 2rpx 8rpx 0 rgba(0, 0, 0, 0.06);

// 基础阴影（悬浮卡片）
box-shadow: 0 4rpx 16rpx 0 rgba(0, 0, 0, 0.08);

// 强阴影（模态框）
box-shadow: 0 8rpx 24rpx 0 rgba(0, 0, 0, 0.10);
```

### 间距系统
- xs: `8rpx` - 小间距
- sm: `12rpx` - 小中间距
- base: `16rpx` - 基础间距
- md: `24rpx` - 中等间距
- lg: `32rpx` - 大间距
- xl: `48rpx` - 超大间距

### 字体系统
```scss
// 字体大小
$font-xs: 20rpx;    // 辅助信息
$font-sm: 24rpx;    // 描述文字
$font-base: 28rpx;  // 正文
$font-lg: 32rpx;    // 小标题
$font-xl: 36rpx;    // 标题
$font-2xl: 40rpx;   // 大标题
$font-3xl: 48rpx;   // 特大标题

// 字体粗细
$font-normal: 400;
$font-medium: 500;
$font-semibold: 600;
$font-bold: 700;
```

## 🏗️ 页面结构

### 1. 英雄区域 (Hero Section)
**设计特点**:
- 渐变背景（绿色渐变：`#34d399` → `#10b981`，温暖治愈系）
- 个性化问候语（动态显示用户名）
- 心理测评入口卡片（白色卡片 + 阴影）

**交互效果**:
- 点击缩放动画：`:active { transform: scale(0.98); }`
- 平滑过渡：`transition: 0.25s cubic-bezier(0.16, 1, 0.3, 1)`

### 2. 心理状态展示
**设计特点**:
- 圆形进度仪表盘（conic-gradient 实现）
- 三个关键指标展示（情绪指数、压力水平、社交质量）
- 根据分值动态显示颜色：
  - ≥80: 翡翠绿 `#10b981`（优秀）
  - ≥60: 薄荷绿 `#34d399`（良好）
  - <60: 金黄色 `#fbbf24`（需关注）

### 3. 今日建议卡片
**设计特点**:
- 纯白背景 + 轻微阴影
- emoji 图标增强视觉趣味
- 行高 1.75，确保良好可读性

### 4. 音乐疗愈模块
**设计特点**:
- 正在播放卡片（绿色渐变背景，带来自然放松感）
- 播放按钮（白色圆形 + 绿色图标）
- 音乐列表（灰色背景卡片）

**交互效果**:
- 播放按钮缩放动画
- 列表项点击反馈（背景变深 + 缩放）

### 5. 文章推荐模块
**设计特点**:
- 左侧 emoji 图标（120rpx × 120rpx）
- 右侧文字信息（标题 + 摘要 + 统计）
- 摘要文本截断（2行省略）

**交互效果**:
- 点击上移动画：`transform: translateY(-2rpx)`
- 阴影增强：从 `shadow-sm` 到 `shadow-base`

## 📁 文件结构

```
MindCampus-App/
├── pages/
│   └── index.vue                   # 首页主文件
├── static/
│   └── scss/
│       ├── theme.scss              # 全局主题变量（新增）
│       └── index.scss              # 全局样式导入
├── pages.json                      # 页面配置（已更新）
└── docs/
    └── HOME_OPTIMIZATION.md        # 本文档
```

## 🔧 核心功能

### 数据驱动
```javascript
data() {
  return {
    userName: '同学',           // 用户名
    mentalScore: 75,            // 心理评分 (0-100)
    mentalLevel: '良好',        // 心理状态等级
    isPlaying: false,           // 音乐播放状态
    indicators: [...],          // 心理指标数据
    musicList: [...],           // 音乐列表
    articleList: [...]          // 文章列表
  }
}
```

### 核心方法

#### 1. 获取用户信息
```javascript
getUserInfo() {
  const name = this.$store.state.user.name
  if (name) {
    this.userName = name
  }
}
```

#### 2. 动态仪表盘渐变
```javascript
getMeterGradient() {
  const percentage = this.mentalScore
  if (percentage >= 80) {
    return 'conic-gradient(#10b981 0% ' + percentage + '%, #f3f4f6 ' + percentage + '% 100%)'
  } else if (percentage >= 60) {
    return 'conic-gradient(#3b82f6 0% ' + percentage + '%, #f3f4f6 ' + percentage + '% 100%)'
  } else {
    return 'conic-gradient(#f59e0b 0% ' + percentage + '%, #f3f4f6 ' + percentage + '% 100%)'
  }
}
```

#### 3. 音乐播放控制
```javascript
// 切换播放状态
toggleMusic() {
  this.isPlaying = !this.isPlaying
  this.$modal.showToast(this.isPlaying ? '开始播放' : '暂停播放')
}

// 播放指定音乐
playMusic(item) {
  this.currentMusic = item
  this.isPlaying = true
  this.$modal.showToast('正在播放：' + item.title)
}
```

## 🚀 快速开始

### 1. 导入主题变量
在需要使用主题变量的 `.vue` 文件中导入：

```vue
<style lang="scss" scoped>
@import '@/static/scss/theme.scss';

.my-component {
  background: $bg-white;
  color: $text-primary;
  border-radius: $radius-md;
  padding: $spacing-md;
}
</style>
```

### 2. 使用全局变量

#### 颜色
```scss
.title {
  color: $text-primary;      // 主标题色
}

.desc {
  color: $text-tertiary;     // 描述文字色
}

.button {
  background: $primary-color; // 主品牌色
}
```

#### 间距
```scss
.card {
  padding: $spacing-md;       // 24rpx
  margin: $spacing-lg;        // 32rpx
}
```

#### 圆角
```scss
.card {
  border-radius: $radius-md;  // 16rpx
}

.avatar {
  border-radius: $radius-full; // 完全圆形
}
```

#### 阴影
```scss
.card {
  box-shadow: $shadow-sm;     // 轻微阴影
}

.modal {
  box-shadow: $shadow-lg;     // 强阴影
}
```

### 3. 数据接口对接

首页预留了 TODO 标记，方便后续对接后端接口：

```javascript
// 打开心理测评
openAssessment() {
  // TODO: 跳转到测评页面
  this.$tab.navigateTo('/pages/assessment/index')
}

// 前往音乐列表
goToMusicList() {
  // TODO: 跳转到音乐列表页面
  this.$tab.navigateTo('/pages/music/list')
}

// 打开文章详情
openArticle(item) {
  // TODO: 跳转到文章详情页
  this.$tab.navigateTo('/pages/article/detail?id=' + item.id)
}
```

## 📱 响应式适配

### 安全区域适配
```scss
// 顶部适配（考虑刘海屏）
.hero-section {
  padding-top: calc($spacing-xl + env(safe-area-inset-top));
}

// 底部适配（考虑 TabBar + 底部安全区）
.home-container {
  padding-bottom: calc(120rpx + env(safe-area-inset-bottom));
}
```

### 多端兼容
- ✅ H5 浏览器
- ✅ 微信小程序
- ✅ Android App
- ✅ iOS App

## 🎯 性能优化

### 1. CSS 优化
- 使用 `transform` 代替 `position` 实现动画
- 使用 `will-change` 提示浏览器优化
- 避免深层嵌套选择器（控制在 3 层内）

### 2. 渲染优化
- 使用 `v-for` 时必须添加 `:key`
- 图片使用懒加载
- 长列表使用虚拟滚动

### 3. 交互优化
- 点击反馈时间 < 150ms
- 页面切换动画流畅（60fps）
- 避免强制同步布局

## 🔍 调试技巧

### 1. 查看样式变量
在浏览器开发者工具中查看计算后的样式值：

```javascript
// 在控制台输入
getComputedStyle(document.querySelector('.assessment-card'))
```

### 2. 调试渐变效果
使用在线工具快速调整：
- [Gradient Generator](https://cssgradient.io/)
- [Conic Gradient Tool](https://www.conic-gradient.com/)

### 3. 测试多端兼容性
```bash
# H5 端
npm run dev:h5

# 微信小程序
npm run dev:mp-weixin

# App 端（需 HBuilderX）
# 使用 HBuilderX 打开项目运行
```

## 📚 设计参考

### 大厂设计规范
- **Apple Human Interface Guidelines**: 简洁、清晰、深度
- **Material Design**: 层次感、动效、响应式
- **Ant Design**: 自然、确定性、意义感
- **Element UI**: 一致性、反馈、效率

### 配色灵感
- [Coolors](https://coolors.co/) - 配色方案生成
- [Flat UI Colors](https://flatuicolors.com/) - 扁平化配色
- [Color Hunt](https://colorhunt.co/) - 流行配色组合

## ⚠️ 注意事项

1. **颜色对比度**: 确保文字与背景对比度 ≥ 4.5:1（符合 WCAG AA 标准）
2. **触摸区域**: 点击区域 ≥ 44px × 44px（约 88rpx × 88rpx）
3. **字体大小**: 正文字体 ≥ 28rpx，确保阅读舒适
4. **动画性能**: 避免动画卡顿，使用 `transform` 和 `opacity` 实现动画
5. **图片优化**: 使用合适尺寸的图片，避免过大图片影响性能

## 🔄 未来扩展

### 待开发功能
- [ ] 心理测评页面
- [ ] 音乐播放器详情页
- [ ] 文章详情页
- [ ] 数据可视化图表
- [ ] 个性化推荐算法

### 可优化点
- [ ] 添加骨架屏加载效果
- [ ] 实现下拉刷新
- [ ] 添加动画过渡效果
- [ ] 深色模式支持
- [ ] 多语言国际化

## 📞 技术支持

如有问题，请联系开发团队或提交 Issue。

---

**版本**: v1.0.0  
**更新日期**: 2025-10-31  
**作者**: MindCampus 开发团队

