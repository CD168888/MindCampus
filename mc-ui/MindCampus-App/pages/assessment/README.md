# 心理测评模块

心理测评问卷列表页面，展示待填和已完成的问卷，采用 MindCampus 主题色系设计。

## 📸 页面预览

```
┌─────────────────────────────────┐
│ ← 心理测评                      │  导航栏
├─────────────────────────────────┤
│  3      5      8                │  统计卡片
│ 待填   已完成   总计             │
├─────────────────────────────────┤
│ 待填问卷                        │
│                                  │
│ ┌─────────────────────────────┐ │
│ │ 📋  抑郁症筛查量表          › │ │
│ │ 评估近两周的情绪状态...      │ │
│ │ 📝 10题 ⏱ 5分钟 👤 张老师  │ │
│ │ 截止时间: 2024-01-25 [待填] │ │
│ └─────────────────────────────┘ │
│                                  │
├─────────────────────────────────┤
│ 已完成                           │
│                                  │
│ ┌─────────────────────────────┐ │
│ │ ✓  睡眠质量评估量表          │ │
│ │ 已于 2024-01-10 完成         │ │
│ │ 📝 7题 📊 68分 👤 张老师    │ │
│ │ [已完成]          [查看结果] │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

## 🎨 设计特点

### 1. 配色方案
完全采用 `theme.scss` 的配色系统：

| 元素 | 颜色 |
|------|------|
| **主色调** | 翡翠绿 (#34d399) |
| **辅助色** | 柔和紫 (#a78bfa) |
| **强调色** | 柔粉 (#fda4af) |
| **背景** | 浅灰 (#f9fafb) |
| **文字** | 深灰系列 (#1f2937 → #9ca3af) |

### 2. 渐变使用

#### 统计卡片顶部边框
```scss
待填问卷: linear-gradient(135deg, #6ee7b7 0%, #a78bfa 100%)  // $gradient-primary
已完成: linear-gradient(135deg, #c4b5fd 0%, #fda4af 100%)    // $gradient-music
总计: linear-gradient(135deg, #fda4af 0%, #6ee7b7 100%)      // 粉到绿
```

#### 问卷图标背景
```scss
问卷1: linear-gradient(135deg, #6ee7b7 0%, #a78bfa 100%)
问卷2: linear-gradient(135deg, #fda4af 0%, #c4b5fd 100%)
问卷3: linear-gradient(135deg, #c4b5fd 0%, #6ee7b7 100%)
```

#### 文字渐变
- 统计数字使用渐变色文字
- 使用 `background-clip: text` 实现

### 3. 视觉层次

#### 导航栏
- 固定顶部，白色背景
- 极淡阴影 ($shadow-xs)
- 左右对称布局

#### 统计卡片
- 三栏等宽布局
- 顶部 3rpx 渐变边框
- 数字大字号 (42rpx) + 渐变色
- 标签小字号 (20rpx) + 灰色
- 点击反馈：上移 2rpx + 阴影增强

#### 问卷卡片
- 白色背景，圆角 24rpx
- 左侧 4rpx 彩色边框（待填问卷）
- 点击反馈：上移 2rpx + 彩色阴影

### 4. 布局规范

| 元素 | 值 |
|------|-----|
| **页面背景** | #f9fafb |
| **导航栏高度** | 88rpx + statusBar |
| **卡片间距** | 20rpx ($spacing-md) |
| **卡片内边距** | 24rpx ($spacing-lg) |
| **卡片圆角** | 24rpx ($radius-lg) |
| **按钮圆角** | 9999rpx ($radius-full) |
| **图标容器** | 72×72rpx |

### 5. 字体规范

| 元素 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 导航标题 | 32rpx | 700 | #1f2937 |
| 统计数字 | 42rpx | 700 | 渐变色 |
| 统计标签 | 20rpx | 500 | #6b7280 |
| 区域标题 | 30rpx | 700 | #1f2937 |
| 卡片标题 | 28rpx | 700 | #1f2937 |
| 卡片描述 | 24rpx | 400 | #6b7280 |
| 元信息 | 20rpx | 500 | #6b7280 |

## 🔧 功能模块

### 1. 统计卡片
```vue
<view class="stat-card stat-pending">
  <view class="stat-number">3</view>
  <view class="stat-label">待填问卷</view>
</view>
```

**功能：**
- 展示待填、已完成、总计数量
- 点击可滚动到对应区域

### 2. 待填问卷卡片
```vue
<view class="questionnaire-card pending-card">
  <view class="card-header">
    <view class="icon-wrapper">📋</view>
    <text class="card-arrow">›</text>
  </view>
  <view class="card-body">
    <view class="card-title">问卷标题</view>
    <view class="card-desc">问卷描述</view>
  </view>
  <view class="card-meta">
    <view class="meta-item">📝 10题</view>
    <view class="meta-item">⏱ 5分钟</view>
    <view class="meta-item">👤 发布人</view>
  </view>
  <view class="card-footer">
    <view class="deadline">截止时间</view>
    <view class="pending-badge">待填写</view>
  </view>
</view>
```

**特点：**
- 左侧彩色边框标识
- 图标背景使用渐变
- 元信息使用 Emoji + 文字
- 右下角"待填写"徽章

### 3. 已完成问卷卡片
```vue
<view class="questionnaire-card completed-card">
  <view class="card-header">
    <view class="icon-wrapper completed-icon">✓</view>
  </view>
  <view class="card-body">
    <view class="card-title">问卷标题</view>
    <view class="completed-time">已于 2024-01-10 完成</view>
  </view>
  <view class="card-meta">
    <view class="meta-item">📝 7题</view>
    <view class="meta-item">📊 得分: 68分</view>
    <view class="meta-item">👤 发布人</view>
  </view>
  <view class="card-footer">
    <view class="completed-badge">已完成</view>
    <view class="view-result-btn">查看结果</view>
  </view>
</view>
```

**特点：**
- 绿色勾选图标
- 显示完成时间和得分
- 右下角"查看结果"按钮（渐变背景）

### 4. 空状态
```vue
<view class="empty-state">
  <text class="empty-icon">✨</text>
  <text class="empty-text">暂无待填问卷</text>
</view>
```

## 📊 数据结构

### 待填问卷
```javascript
{
  id: 1,
  icon: '📋',
  iconBg: 'linear-gradient(135deg, #6ee7b7 0%, #a78bfa 100%)',
  title: '抑郁症筛查量表 (PHQ-9)',
  description: '评估近两周的情绪状态，帮助识别抑郁症状',
  questionCount: 10,
  duration: 5,
  publisher: '张老师',
  deadline: '2024-01-25'
}
```

### 已完成问卷
```javascript
{
  id: 101,
  title: '睡眠质量评估量表',
  completedTime: '2024-01-10',
  questionCount: 7,
  score: 68,
  publisher: '张老师'
}
```

## 🎯 交互逻辑

### 1. 页面加载
```javascript
onLoad() {
  // 获取状态栏高度（适配刘海屏）
  const systemInfo = uni.getSystemInfoSync()
  this.statusBarHeight = systemInfo.statusBarHeight || 0
  
  // 加载问卷数据
  this.loadQuestionnaires()
}
```

### 2. 统计卡片点击
```javascript
scrollToSection(section) {
  // 滚动到指定区域（待填/已完成）
  uni.pageScrollTo({
    scrollTop: res[0].top - 100,
    duration: 300
  })
}
```

### 3. 开始填写问卷
```javascript
startQuestionnaire(item) {
  uni.navigateTo({
    url: '/pages/assessment/detail?id=' + item.id
  })
}
```

### 4. 查看结果
```javascript
viewResult(item) {
  uni.navigateTo({
    url: '/pages/assessment/result?id=' + item.id
  })
}
```

## 🔗 页面跳转

### 从首页进入
```javascript
// pages/index.vue
openAssessment() {
  uni.navigateTo({
    url: '/pages/assessment/list'
  })
}
```

### 跳转到问卷详情
```javascript
// TODO: 创建问卷详情页
uni.navigateTo({
  url: '/pages/assessment/detail?id=' + item.id
})
```

### 跳转到结果页
```javascript
// TODO: 创建结果查看页
uni.navigateTo({
  url: '/pages/assessment/result?id=' + item.id
})
```

## 📱 响应式设计

### 状态栏适配
```vue
<view class="custom-navbar" :style="{ paddingTop: statusBarHeight + 'px' }">
```

### 安全区域适配
- 底部留出足够间距，避免被系统手势遮挡
- 使用 `env(safe-area-inset-bottom)` 或手动设置

## 🎨 视觉效果对比

| 特性 | 实现方式 |
|------|----------|
| **顶部导航** | 固定定位 + 极淡阴影 |
| **统计卡片** | 顶部彩色条 + 渐变文字 |
| **待填卡片** | 左侧彩色边框 + 点击彩色阴影 |
| **已完成卡片** | 绿色勾选图标 + 降低不透明度 |
| **按钮** | 渐变背景 + 圆角胶囊 |
| **空状态** | Emoji + 文字居中 |

## 🚀 后续扩展

### 1. 筛选功能
- 按类型筛选（抑郁、焦虑、压力等）
- 按状态筛选（全部、待填、已完成）
- 按时间排序

### 2. 搜索功能
- 顶部添加搜索框
- 支持问卷名称搜索

### 3. 下拉刷新
```javascript
onPullDownRefresh() {
  this.loadQuestionnaires()
  uni.stopPullDownRefresh()
}
```

### 4. 上拉加载更多
```javascript
onReachBottom() {
  this.loadMore()
}
```

### 5. 骨架屏加载
- 数据加载时显示骨架屏
- 提升用户体验

### 6. 分享功能
- 分享问卷给好友
- 生成问卷海报

## 🔧 API 接口设计

### 获取问卷列表
```javascript
GET /api/questionnaires

Response:
{
  "code": 200,
  "data": {
    "pending": [...],
    "completed": [...],
    "stats": {
      "pending": 3,
      "completed": 5,
      "total": 8
    }
  }
}
```

### 开始填写问卷
```javascript
POST /api/questionnaires/{id}/start

Response:
{
  "code": 200,
  "data": {
    "sessionId": "xxx",
    "questions": [...]
  }
}
```

### 查看问卷结果
```javascript
GET /api/questionnaires/{id}/result

Response:
{
  "code": 200,
  "data": {
    "score": 68,
    "level": "良好",
    "analysis": "...",
    "suggestions": [...]
  }
}
```

## 💡 设计亮点

1. ✨ **统一主题色系**：完全采用 `theme.scss` 配色
2. 🎨 **渐变运用**：多处使用渐变色增强视觉吸引力
3. 📊 **信息层次**：清晰的视觉层次，重点突出
4. 🎯 **交互反馈**：点击、悬停都有明确的视觉反馈
5. 🌈 **个性图标**：每个问卷不同的渐变背景和图标
6. 📱 **响应式布局**：适配各种屏幕尺寸
7. 🎭 **状态区分**：待填、已完成视觉明显区分
8. ✨ **空状态友好**：空状态使用 Emoji 营造轻松氛围

