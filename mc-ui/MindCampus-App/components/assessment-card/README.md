# 心理测评卡片组件 (AssessmentCard)

一个现代化、视觉吸引力强的心理测评入口卡片组件，采用渐变背景、动态装饰元素和流畅动画。

## 📸 效果预览

```
┌────────────────────────────────────┐
│  📋   [开始测评]                   │
│                                     │
│  心理健康测评                       │
│  了解你的心理健康状况，获取专业建议  │
│                                     │
│                  [立即开始 →]       │
└────────────────────────────────────┘
     ↑ 彩色渐变背景 + 漂浮装饰圆圈
```

## 🚀 使用方法

### 基础用法

```vue
<template>
  <assessment-card @click="openAssessment"></assessment-card>
</template>

<script>
import AssessmentCard from '@/components/assessment-card/assessment-card.vue'

export default {
  components: {
    AssessmentCard
  },
  methods: {
    openAssessment() {
      uni.navigateTo({
        url: '/pages/assessment/index'
      })
    }
  }
}
</script>
```

### 自定义内容

```vue
<template>
  <assessment-card 
    icon="🧠" 
    title="压力测评" 
    subtitle="评估你的压力水平，找到缓解方法"
    @click="handleClick"
  ></assessment-card>
</template>

<script>
import AssessmentCard from '@/components/assessment-card/assessment-card.vue'

export default {
  components: {
    AssessmentCard
  },
  methods: {
    handleClick() {
      console.log('点击了测评卡片')
    }
  }
}
</script>
```

## 📝 Props

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| icon | String | '📋' | 卡片图标（支持 Emoji） |
| title | String | '心理健康测评' | 卡片标题 |
| subtitle | String | '了解你的心理健康状况，获取专业建议' | 卡片副标题 |

## 🎯 Events

| 事件名 | 说明 | 回调参数 |
|--------|------|----------|
| click | 点击卡片时触发 | - |

## 🎨 设计特点

### 视觉效果

#### 1. **渐变背景**
- 使用三色渐变：翡翠绿 → 柔和紫 → 柔粉
- 渐变角度：135度
- 颜色过渡自然流畅

```scss
background: linear-gradient(135deg, 
  #6ee7b7 0%,   // 翡翠绿
  #a78bfa 50%,  // 柔和紫
  #fda4af 100%  // 柔粉
);
```

#### 2. **动态装饰元素**
- 3个漂浮的半透明圆圈
- 不同大小和位置
- 独立的浮动动画
- 增强视觉层次感

| 圆圈 | 大小 | 位置 | 动画时长 |
|------|------|------|----------|
| circle-1 | 200rpx | 右上角 | 6秒 |
| circle-2 | 150rpx | 左下角 | 5秒 |
| circle-3 | 100rpx | 中部 | 7秒 |

#### 3. **玻璃态设计**
- 图标容器采用毛玻璃效果
- 半透明白色背景
- `backdrop-filter: blur(10rpx)`
- 微妙的白色边框

#### 4. **徽章标签**
- 右上角"开始测评"标签
- 白色背景，圆角胶囊造型
- 渐变色文字（使用 `background-clip: text`）
- 柔和阴影

#### 5. **行动按钮**
- 右下角"立即开始"按钮
- 白色背景，圆角胶囊造型
- 渐变色文字 + 箭头图标
- 点击时向右移动

### 交互效果

#### 1. **点击反馈**
```scss
&:active {
  transform: scale(0.98);      // 轻微缩小
  box-shadow: $shadow-md;      // 阴影减弱
}
```

#### 2. **按钮动画**
```scss
.assessment-card:active .action-btn {
  background: rgba(255, 255, 255, 1);  // 背景变白
  transform: translateX(4rpx);          // 向右移动
}
```

#### 3. **漂浮动画**
- 3个装饰圆圈独立动画
- 不同的移动方向和缩放比例
- 无限循环，ease-in-out 缓动

```scss
@keyframes float1 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(-10rpx, 10rpx) scale(1.1); }
}
```

### 技术实现

#### 1. **层级管理**
```
┌─ card-background (z-index: 0) ─ 渐变背景层
│  ├─ deco-circle × 3          ─ 装饰圆圈
│
└─ card-content (z-index: 1)   ─ 内容层
   ├─ card-header              ─ 头部（图标+徽章）
   ├─ card-body                ─ 主体（标题+副标题）
   └─ card-footer              ─ 底部（行动按钮）
```

#### 2. **响应式布局**
- Flexbox 布局
- 使用主题变量控制间距
- 自适应高度（最小 180rpx）

#### 3. **性能优化**
- CSS 动画使用 `transform`（GPU 加速）
- 合理使用 `will-change` 隐式声明
- 避免触发重排（layout）

## 📐 尺寸规格

| 属性 | 值 | 说明 |
|------|-----|------|
| 最小高度 | 180rpx | 保证内容完整显示 |
| 圆角 | 32rpx | $radius-xl |
| 阴影 | $shadow-lg | 大型阴影 |
| 图标容器 | 64×64rpx | 正方形 |
| 图标大小 | 40rpx | Emoji |
| 标题字号 | 32rpx | $font-xl |
| 副标题字号 | 24rpx | $font-sm |
| 按钮 padding | 12rpx 24rpx | 胶囊造型 |

## 🎨 颜色系统

### 渐变背景
```scss
linear-gradient(135deg, 
  #6ee7b7 0%,   // 翡翠绿（主色调明亮版）
  #a78bfa 50%,  // 柔和紫（辅助色）
  #fda4af 100%  // 柔粉（强调色）
)
```

### 文字颜色
- 标题：白色 (#FFFFFF)
- 副标题：白色 92% 透明度 (rgba(255, 255, 255, 0.92))
- 徽章文字：渐变色（$gradient-primary）
- 按钮文字：渐变色（$gradient-primary）

### 装饰元素
- 圆圈：白色 25% 透明度径向渐变
- 玻璃容器背景：白色 25% 透明度
- 玻璃容器边框：白色 30% 透明度
- 徽章背景：白色 90% 透明度
- 按钮背景：白色 95% 透明度

## 🔧 自定义样式

### 修改渐变背景

```vue
<style lang="scss" scoped>
/* 覆盖渐变背景 */
.card-background {
  background: linear-gradient(135deg, 
    #your-color-1 0%, 
    #your-color-2 50%, 
    #your-color-3 100%
  ) !important;
}
</style>
```

### 修改卡片高度

```vue
<style lang="scss" scoped>
.assessment-card {
  min-height: 200rpx !important;
}
</style>
```

### 禁用动画

```vue
<style lang="scss" scoped>
.deco-circle {
  animation: none !important;
}
</style>
```

## 💡 使用建议

1. **位置摆放**：建议放在首页顶部或显著位置，作为核心功能入口
2. **图标选择**：使用与心理测评相关的 Emoji（如 📋 📝 🧠 💭 🎯）
3. **文案优化**：标题简短有力，副标题说明具体价值
4. **跳转逻辑**：点击后跳转到测评页面或先展示测评介绍
5. **数据追踪**：可以在 `@click` 事件中添加埋点，跟踪点击率

## 🌟 进阶功能

### 动态数据驱动

```vue
<template>
  <assessment-card 
    v-for="item in assessmentList" 
    :key="item.id"
    :icon="item.icon"
    :title="item.title"
    :subtitle="item.subtitle"
    @click="openAssessment(item)"
  ></assessment-card>
</template>

<script>
export default {
  data() {
    return {
      assessmentList: [
        { id: 1, icon: '📋', title: '综合评估', subtitle: '全面了解心理状态' },
        { id: 2, icon: '😰', title: '焦虑测评', subtitle: '评估焦虑水平' },
        { id: 3, icon: '😔', title: '抑郁测评', subtitle: '检测抑郁倾向' }
      ]
    }
  },
  methods: {
    openAssessment(item) {
      uni.navigateTo({
        url: `/pages/assessment/detail?id=${item.id}`
      })
    }
  }
}
</script>
```

### 添加完成状态

可以扩展组件，添加"已完成"状态显示：

```vue
<assessment-card 
  icon="📋" 
  title="心理健康测评" 
  subtitle="了解你的心理健康状况"
  :completed="true"
  @click="viewResult"
></assessment-card>
```

### 添加倒计时

提示用户上次测评时间，建议定期重测：

```vue
<assessment-card 
  icon="📋" 
  title="心理健康测评" 
  subtitle="距上次测评已过 7 天，建议重新测评"
  @click="openAssessment"
></assessment-card>
```

## 📊 对比旧设计

| 特性 | 旧设计 | 新设计 |
|------|--------|--------|
| **背景** | 玻璃态（半透明白） | 彩色渐变 |
| **布局** | 水平排列 | 垂直层叠 |
| **视觉层次** | 平面 | 立体（多层元素） |
| **动画** | 简单位移 | 多元素浮动 |
| **吸引力** | 低调内敛 | 醒目活泼 |
| **点击反馈** | 基础缩放 | 多重反馈 |
| **徽章** | 无 | 有（右上角） |
| **行动按钮** | 箭头 | 明确的"立即开始" |

新设计更能吸引用户注意力，提高点击率！

