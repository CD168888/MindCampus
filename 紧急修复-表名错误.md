# 🔥 紧急修复：表名映射错误

## 问题描述

运行时报错：
```
Table 'ry-vue.student' doesn't exist
```

## 原因分析

`Student` 实体类缺少 `@TableName` 注解，MyBatis Plus 默认使用类名的下划线形式作为表名：
- 类名：`Student`
- 默认表名：`student`（❌ 错误）
- 实际表名：`student_info`（✅ 正确）

## 解决方案

在 `Student.java` 文件中添加 `@TableName("student_info")` 注解。

### 修改位置
**文件**: `mc-project/src/main/java/com/mc/student/domain/Student.java`

### 修改前
```java
@Data
@EqualsAndHashCode(callSuper = true)
@ToString(callSuper = true)
@Schema(description = "学生信息实体")
public class Student extends BaseEntity {
```

### 修改后
```java
import com.baomidou.mybatisplus.annotation.TableName;  // ← 新增导入

@Data
@TableName("student_info")  // ← 新增注解，指定正确的表名
@EqualsAndHashCode(callSuper = true)
@ToString(callSuper = true)
@Schema(description = "学生信息实体")
public class Student extends BaseEntity {
```

## 验证方法

1. 重新编译项目
2. 重启应用
3. 访问测评相关接口，应该不再报 `Table 'student' doesn't exist` 错误

## 相关SQL

修复后，MyBatis Plus 会生成正确的SQL：
```sql
SELECT student_id, user_id, student_no, name, gender, grade, major, 
       class_name, phone, status, search_value, create_by, create_time, 
       update_by, update_time, remark, params 
FROM student_info     -- ✅ 现在是正确的表名
WHERE (user_id = ?)
```

## 影响范围

- ✅ 所有使用 `StudentInfoMapper` 的查询
- ✅ 通过 userId 查询 studentId 的逻辑
- ✅ APP端心理测评模块的所有接口

## 状态

✅ **已修复** - 2025-11-04

---

**注意**: 如果您的项目中还有其他实体类，请检查是否也需要添加 `@TableName` 注解！

